/**
 * Bump this value whenever setup-almostnode changes the copied runtime bundle.
 * The query prevents a browser session from retaining an earlier module instance.
 */
export const ALMOSTNODE_RUNTIME_URL = '/lib/almostnode/index.mjs?zakamurai-patch=2';
